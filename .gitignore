# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results/
/playwright-report/

# next.js
/.next/
# per-session preview dist dirs (NEXT_DIST_DIR in .claude/launch.json) —
# must stay gitignored or Tailwind v4's content scanner reads their build
# chunks and emits corrupted utilities (CSS parse failure).
/.next-preview*/
/out/

# production
/build

# misc
.claude/
.DS_Store
**/.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# per-session preview dist dirs (NEXT_DIST_DIR)
/.next-preview*/
