# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results/
/playwright-report/

# next.js
/.next/
# Any custom NEXT_DIST_DIR / verify caches (.next-preview*, .next-tn-verify,
# .next-trust-narrow, …). Must stay gitignored or Tailwind v4's content
# scanner reads Turbopack binary chunks and emits corrupted utilities
# (e.g. .[key:�$4ng] → CssSyntaxError on app/globals.css).
/.next-*/
/out/

# production
/build

# misc
.claude/
.DS_Store
**/.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# per-session preview dist dirs (NEXT_DIST_DIR) — covered by /.next-*/ above
